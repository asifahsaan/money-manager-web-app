import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asifahsaan.moneymanager',
  appName: 'Money Manager',
  webDir: 'dist'
};

export default config;
